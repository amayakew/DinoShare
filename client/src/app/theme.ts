import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#689660',
      light: '#95b58f',
      dark: '#293c26',
      contrastText: '#fff',
    },
  },
});

export default theme;